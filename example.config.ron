(
    // Viewport settings
    viewport_width: 1024,
    viewport_height: 1024,

    // Voxel settings
    voxel_dimension: 128, // Has to satisfy: 2^{octree_levels - 1}
    brick_pool_resolution: 210,

    // Octree settings
    octree_levels: 8,

    // Compute shader settings
    working_group_size: 64,
)