[package]
name = "macros"
description = "Procedural macros for the core of the project"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = "2"
proc-macro2 = "1"
quote = "1"

[dev-dependencies]
trybuild = { version = "1.0.74", features = ["diff"] }
